name: vpn_client
description: SD-WAN VPN client scaffold
publish_to: none
version: 0.1.1+1125

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  logger: ^2.6.2
  flutter_secure_storage: ^9.2.4
  uuid: ^4.5.1
  url_launcher: ^6.3.1
  app_links: ^6.3.0
  shared_preferences: ^2.3.4
  webview_windows: ^0.4.0
  webview_flutter: ^4.10.0
  window_manager: ^0.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
